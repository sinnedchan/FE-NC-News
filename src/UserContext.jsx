import React from "react";

// export default function userContext() {
//   React.createContext({ username: {} });
// }

const UserContext = React.createContext({ username: {} });
export default UserContext;
